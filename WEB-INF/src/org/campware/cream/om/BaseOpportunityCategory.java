package org.campware.cream.om;


import java.math.BigDecimal;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Date;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.turbine.om.Retrievable;
import org.apache.torque.TorqueException;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;


/**
 * This class was autogenerated by Torque on:
 *
 * [Fri Jan 26 03:28:50 CET 2007]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to OpportunityCategory
 */
public abstract class BaseOpportunityCategory extends BaseObject
    implements org.apache.turbine.om.Retrievable
{
    /** The Peer class */
    private static final OpportunityCategoryPeer peer =
        new OpportunityCategoryPeer();

        
    /** The value for the opportunityCatId field */
    private int opportunityCatId;
      
    /** The value for the opportunityCatName field */
    private String opportunityCatName;
  
    
    /**
     * Get the OpportunityCatId
     *
     * @return int
     */
    public int getOpportunityCatId()
    {
        return opportunityCatId;
    }

                                              
    /**
     * Set the value of OpportunityCatId
     *
     * @param v new value
     */
    public void setOpportunityCatId(int v) throws TorqueException
    {
    
                  if (this.opportunityCatId != v)
              {
            this.opportunityCatId = v;
            setModified(true);
        }
    
          
                                  
                  // update associated Opportunity
        if (collOpportunitys != null)
        {
            for (int i = 0; i < collOpportunitys.size(); i++)
            {
                ((Opportunity) collOpportunitys.get(i))
                    .setOpportunityCatId(v);
            }
        }
                                }
  
    /**
     * Get the OpportunityCatName
     *
     * @return String
     */
    public String getOpportunityCatName()
    {
        return opportunityCatName;
    }

                        
    /**
     * Set the value of OpportunityCatName
     *
     * @param v new value
     */
    public void setOpportunityCatName(String v) 
    {
    
                  if (!ObjectUtils.equals(this.opportunityCatName, v))
              {
            this.opportunityCatName = v;
            setModified(true);
        }
    
          
              }
  
         
                                
            
          /**
     * Collection to store aggregation of collOpportunitys
     */
    protected List collOpportunitys;

    /**
     * Temporary storage of collOpportunitys to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initOpportunitys()
    {
        if (collOpportunitys == null)
        {
            collOpportunitys = new ArrayList();
        }
    }

    /**
     * Method called to associate a Opportunity object to this object
     * through the Opportunity foreign key attribute
     *
     * @param l Opportunity
     * @throws TorqueException
     */
    public void addOpportunity(Opportunity l) throws TorqueException
    {
        getOpportunitys().add(l);
        l.setOpportunityCategory((OpportunityCategory) this);
    }

    /**
     * The criteria used to select the current contents of collOpportunitys
     */
    private Criteria lastOpportunitysCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getOpportunitys(new Criteria())
     *
     * @throws TorqueException
     */
    public List getOpportunitys() throws TorqueException
    {
              if (collOpportunitys == null)
        {
            collOpportunitys = getOpportunitys(new Criteria(10));
        }
        return collOpportunitys;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OpportunityCategory has previously
     * been saved, it will retrieve related Opportunitys from storage.
     * If this OpportunityCategory is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getOpportunitys(Criteria criteria) throws TorqueException
    {
              if (collOpportunitys == null)
        {
            if (isNew())
            {
               collOpportunitys = new ArrayList();
            }
            else
            {
                        criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId() );
                        collOpportunitys = OpportunityPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                            if (!lastOpportunitysCriteria.equals(criteria))
                {
                    collOpportunitys = OpportunityPeer.doSelect(criteria);
                }
            }
        }
        lastOpportunitysCriteria = criteria;

        return collOpportunitys;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getOpportunitys(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getOpportunitys(Connection con) throws TorqueException
    {
              if (collOpportunitys == null)
        {
            collOpportunitys = getOpportunitys(new Criteria(10), con);
        }
        return collOpportunitys;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OpportunityCategory has previously
     * been saved, it will retrieve related Opportunitys from storage.
     * If this OpportunityCategory is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getOpportunitys(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collOpportunitys == null)
        {
            if (isNew())
            {
               collOpportunitys = new ArrayList();
            }
            else
            {
                         criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                         collOpportunitys = OpportunityPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                             if (!lastOpportunitysCriteria.equals(criteria))
                 {
                     collOpportunitys = OpportunityPeer.doSelect(criteria, con);
                 }
             }
         }
         lastOpportunitysCriteria = criteria;

         return collOpportunitys;
           }

                                          
              
                    
                    
                                
                                                              
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OpportunityCategory is new, it will return
     * an empty collection; or if this OpportunityCategory has previously
     * been saved, it will retrieve related Opportunitys from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OpportunityCategory.
     */
    protected List getOpportunitysJoinCustomer(Criteria criteria)
        throws TorqueException
    {
                    if (collOpportunitys == null)
        {
            if (isNew())
            {
               collOpportunitys = new ArrayList();
            }
            else
            {
                              criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                              collOpportunitys = OpportunityPeer.doSelectJoinCustomer(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                                    if (!lastOpportunitysCriteria.equals(criteria))
            {
                collOpportunitys = OpportunityPeer.doSelectJoinCustomer(criteria);
            }
        }
        lastOpportunitysCriteria = criteria;

        return collOpportunitys;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OpportunityCategory is new, it will return
     * an empty collection; or if this OpportunityCategory has previously
     * been saved, it will retrieve related Opportunitys from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OpportunityCategory.
     */
    protected List getOpportunitysJoinProject(Criteria criteria)
        throws TorqueException
    {
                    if (collOpportunitys == null)
        {
            if (isNew())
            {
               collOpportunitys = new ArrayList();
            }
            else
            {
                              criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                              collOpportunitys = OpportunityPeer.doSelectJoinProject(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                                    if (!lastOpportunitysCriteria.equals(criteria))
            {
                collOpportunitys = OpportunityPeer.doSelectJoinProject(criteria);
            }
        }
        lastOpportunitysCriteria = criteria;

        return collOpportunitys;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OpportunityCategory is new, it will return
     * an empty collection; or if this OpportunityCategory has previously
     * been saved, it will retrieve related Opportunitys from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OpportunityCategory.
     */
    protected List getOpportunitysJoinCurrency(Criteria criteria)
        throws TorqueException
    {
                    if (collOpportunitys == null)
        {
            if (isNew())
            {
               collOpportunitys = new ArrayList();
            }
            else
            {
                              criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                              collOpportunitys = OpportunityPeer.doSelectJoinCurrency(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                                    if (!lastOpportunitysCriteria.equals(criteria))
            {
                collOpportunitys = OpportunityPeer.doSelectJoinCurrency(criteria);
            }
        }
        lastOpportunitysCriteria = criteria;

        return collOpportunitys;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OpportunityCategory is new, it will return
     * an empty collection; or if this OpportunityCategory has previously
     * been saved, it will retrieve related Opportunitys from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OpportunityCategory.
     */
    protected List getOpportunitysJoinLeadSource(Criteria criteria)
        throws TorqueException
    {
                    if (collOpportunitys == null)
        {
            if (isNew())
            {
               collOpportunitys = new ArrayList();
            }
            else
            {
                              criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                              collOpportunitys = OpportunityPeer.doSelectJoinLeadSource(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                                    if (!lastOpportunitysCriteria.equals(criteria))
            {
                collOpportunitys = OpportunityPeer.doSelectJoinLeadSource(criteria);
            }
        }
        lastOpportunitysCriteria = criteria;

        return collOpportunitys;
                }
                  
                    
                              
                                
                                                              
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OpportunityCategory is new, it will return
     * an empty collection; or if this OpportunityCategory has previously
     * been saved, it will retrieve related Opportunitys from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OpportunityCategory.
     */
    protected List getOpportunitysJoinOpportunityCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collOpportunitys == null)
        {
            if (isNew())
            {
               collOpportunitys = new ArrayList();
            }
            else
            {
                              criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                              collOpportunitys = OpportunityPeer.doSelectJoinOpportunityCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(OpportunityPeer.OPPORTUNITY_CAT_ID, getOpportunityCatId());
                                    if (!lastOpportunitysCriteria.equals(criteria))
            {
                collOpportunitys = OpportunityPeer.doSelectJoinOpportunityCategory(criteria);
            }
        }
        lastOpportunitysCriteria = criteria;

        return collOpportunitys;
                }
                            


          
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
              fieldNames.add("OpportunityCatId");
              fieldNames.add("OpportunityCatName");
              fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
          if (name.equals("OpportunityCatId"))
        {
                return new Integer(getOpportunityCatId());
            }
          if (name.equals("OpportunityCatName"))
        {
                return getOpportunityCatName();
            }
          return null;
    }
    
    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
          if (name.equals(OpportunityCategoryPeer.OPPORTUNITY_CAT_ID))
        {
                return new Integer(getOpportunityCatId());
            }
          if (name.equals(OpportunityCategoryPeer.OPPORTUNITY_CAT_NAME))
        {
                return getOpportunityCatName();
            }
          return null;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
            if (pos == 0)
        {
                return new Integer(getOpportunityCatId());
            }
              if (pos == 1)
        {
                return getOpportunityCatName();
            }
              return null;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
          save(OpportunityCategoryPeer.getMapBuilder()
                .getDatabaseMap().getName());
      }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
       * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
       *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
          try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
      }

      /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
          if (!alreadyInSave)
        {
            alreadyInSave = true;


  
            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    OpportunityCategoryPeer.doInsert((OpportunityCategory) this, con);
                    setNew(false);
                }
                else
                {
                    OpportunityCategoryPeer.doUpdate((OpportunityCategory) this, con);
                }
            }

                                      
                
                    if (collOpportunitys != null)
            {
                for (int i = 0; i < collOpportunitys.size(); i++)
                {
                    ((Opportunity) collOpportunitys.get(i)).save(con);
                }
            }
                                  alreadyInSave = false;
        }
      }

                        
      /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param key opportunityCatId ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        throws TorqueException
    {
            setOpportunityCatId(((NumberKey) key).intValue());
        }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
            setOpportunityCatId(Integer.parseInt(key));
        }

  
    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
          return SimpleKey.keyFor(getOpportunityCatId());
      }
 
    /**
     * get an id that differentiates this object from others
     * of its class.
     */
    public String getQueryKey()
    {
        if (getPrimaryKey() == null)
        {
            return "";
        }
        else
        {
            return getPrimaryKey().toString();
        }
    }

    /**
     * set an id that differentiates this object from others
     * of its class.
     */
    public void setQueryKey(String key)
        throws TorqueException
    {
        setPrimaryKey(key);
    }

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
       * It then fills all the association collections and sets the
     * related objects to isNew=true.
       */
      public OpportunityCategory copy() throws TorqueException
    {
        return copyInto(new OpportunityCategory());
    }
  
    protected OpportunityCategory copyInto(OpportunityCategory copyObj) throws TorqueException
    {
          copyObj.setOpportunityCatId(opportunityCatId);
          copyObj.setOpportunityCatName(opportunityCatName);
  
                            copyObj.setOpportunityCatId( 0);
                  
                                      
                            
        List v = getOpportunitys();
        for (int i = 0; i < v.size(); i++)
        {
            Opportunity obj = (Opportunity) v.get(i);
            copyObj.addOpportunity(obj.copy());
        }
                            return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public OpportunityCategoryPeer getPeer()
    {
        return peer;
    }

    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("OpportunityCategory:\n");
        str.append("OpportunityCatId = ")
               .append(getOpportunityCatId())
             .append("\n");
        str.append("OpportunityCatName = ")
               .append(getOpportunityCatName())
             .append("\n");
        return(str.toString());
    }
}
