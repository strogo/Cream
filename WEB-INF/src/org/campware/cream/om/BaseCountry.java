package org.campware.cream.om;


import java.math.BigDecimal;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Date;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.turbine.om.Retrievable;
import org.apache.torque.TorqueException;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;


/**
 * This class was autogenerated by Torque on:
 *
 * [Fri Jan 26 03:28:50 CET 2007]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to Country
 */
public abstract class BaseCountry extends BaseObject
    implements org.apache.turbine.om.Retrievable
{
    /** The Peer class */
    private static final CountryPeer peer =
        new CountryPeer();

        
    /** The value for the countryId field */
    private int countryId;
      
    /** The value for the countryName field */
    private String countryName;
      
    /** The value for the countryCode field */
    private String countryCode;
  
    
    /**
     * Get the CountryId
     *
     * @return int
     */
    public int getCountryId()
    {
        return countryId;
    }

                                              
    /**
     * Set the value of CountryId
     *
     * @param v new value
     */
    public void setCountryId(int v) throws TorqueException
    {
    
                  if (this.countryId != v)
              {
            this.countryId = v;
            setModified(true);
        }
    
          
                                  
                  // update associated Customer
        if (collCustomersRelatedByCountryId != null)
        {
            for (int i = 0; i < collCustomersRelatedByCountryId.size(); i++)
            {
                ((Customer) collCustomersRelatedByCountryId.get(i))
                    .setCountryId(v);
            }
        }
                                                    
                  // update associated Customer
        if (collCustomersRelatedByShiptoCountryId != null)
        {
            for (int i = 0; i < collCustomersRelatedByShiptoCountryId.size(); i++)
            {
                ((Customer) collCustomersRelatedByShiptoCountryId.get(i))
                    .setShiptoCountryId(v);
            }
        }
                                                    
                  // update associated Contact
        if (collContactsRelatedByCountryId != null)
        {
            for (int i = 0; i < collContactsRelatedByCountryId.size(); i++)
            {
                ((Contact) collContactsRelatedByCountryId.get(i))
                    .setCountryId(v);
            }
        }
                                                    
                  // update associated Contact
        if (collContactsRelatedBySecCountryId != null)
        {
            for (int i = 0; i < collContactsRelatedBySecCountryId.size(); i++)
            {
                ((Contact) collContactsRelatedBySecCountryId.get(i))
                    .setSecCountryId(v);
            }
        }
                                                    
                  // update associated Newsletter
        if (collNewsletters != null)
        {
            for (int i = 0; i < collNewsletters.size(); i++)
            {
                ((Newsletter) collNewsletters.get(i))
                    .setCustCountryId(v);
            }
        }
                                }
  
    /**
     * Get the CountryName
     *
     * @return String
     */
    public String getCountryName()
    {
        return countryName;
    }

                        
    /**
     * Set the value of CountryName
     *
     * @param v new value
     */
    public void setCountryName(String v) 
    {
    
                  if (!ObjectUtils.equals(this.countryName, v))
              {
            this.countryName = v;
            setModified(true);
        }
    
          
              }
  
    /**
     * Get the CountryCode
     *
     * @return String
     */
    public String getCountryCode()
    {
        return countryCode;
    }

                        
    /**
     * Set the value of CountryCode
     *
     * @param v new value
     */
    public void setCountryCode(String v) 
    {
    
                  if (!ObjectUtils.equals(this.countryCode, v))
              {
            this.countryCode = v;
            setModified(true);
        }
    
          
              }
  
         
                                        
            
          /**
     * Collection to store aggregation of collCustomersRelatedByCountryId
     */
    protected List collCustomersRelatedByCountryId;

    /**
     * Temporary storage of collCustomersRelatedByCountryId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initCustomersRelatedByCountryId()
    {
        if (collCustomersRelatedByCountryId == null)
        {
            collCustomersRelatedByCountryId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Customer object to this object
     * through the Customer foreign key attribute
     *
     * @param l Customer
     * @throws TorqueException
     */
    public void addCustomerRelatedByCountryId(Customer l) throws TorqueException
    {
        getCustomersRelatedByCountryId().add(l);
        l.setCountryRelatedByCountryId((Country) this);
    }

    /**
     * The criteria used to select the current contents of collCustomersRelatedByCountryId
     */
    private Criteria lastCustomersRelatedByCountryIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByCountryId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByCountryId() throws TorqueException
    {
              if (collCustomersRelatedByCountryId == null)
        {
            collCustomersRelatedByCountryId = getCustomersRelatedByCountryId(new Criteria(10));
        }
        return collCustomersRelatedByCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByCountryId(Criteria criteria) throws TorqueException
    {
              if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId() );
                        collCustomersRelatedByCountryId = CustomerPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                            if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
                {
                    collCustomersRelatedByCountryId = CustomerPeer.doSelect(criteria);
                }
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByCountryId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByCountryId(Connection con) throws TorqueException
    {
              if (collCustomersRelatedByCountryId == null)
        {
            collCustomersRelatedByCountryId = getCustomersRelatedByCountryId(new Criteria(10), con);
        }
        return collCustomersRelatedByCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByCountryId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                         criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                         collCustomersRelatedByCountryId = CustomerPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                             if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
                 {
                     collCustomersRelatedByCountryId = CustomerPeer.doSelect(criteria, con);
                 }
             }
         }
         lastCustomersRelatedByCountryIdCriteria = criteria;

         return collCustomersRelatedByCountryId;
           }

                                                                                          
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinCustomerCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinIndustry(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinLeadSource(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinRegionRelatedByRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinCountryRelatedByShiptoCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinCountryRelatedByShiptoCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinCountryRelatedByShiptoCountryId(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinRegionRelatedByShiptoRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinRegionRelatedByShiptoRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinRegionRelatedByShiptoRegionId(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinEducationCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinHouseholdCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinAgeCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinRevenueCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByCountryIdJoinEmployeeNoCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                              collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByCountryId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        lastCustomersRelatedByCountryIdCriteria = criteria;

        return collCustomersRelatedByCountryId;
                }
                            


                                  
            
          /**
     * Collection to store aggregation of collCustomersRelatedByShiptoCountryId
     */
    protected List collCustomersRelatedByShiptoCountryId;

    /**
     * Temporary storage of collCustomersRelatedByShiptoCountryId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initCustomersRelatedByShiptoCountryId()
    {
        if (collCustomersRelatedByShiptoCountryId == null)
        {
            collCustomersRelatedByShiptoCountryId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Customer object to this object
     * through the Customer foreign key attribute
     *
     * @param l Customer
     * @throws TorqueException
     */
    public void addCustomerRelatedByShiptoCountryId(Customer l) throws TorqueException
    {
        getCustomersRelatedByShiptoCountryId().add(l);
        l.setCountryRelatedByShiptoCountryId((Country) this);
    }

    /**
     * The criteria used to select the current contents of collCustomersRelatedByShiptoCountryId
     */
    private Criteria lastCustomersRelatedByShiptoCountryIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByShiptoCountryId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByShiptoCountryId() throws TorqueException
    {
              if (collCustomersRelatedByShiptoCountryId == null)
        {
            collCustomersRelatedByShiptoCountryId = getCustomersRelatedByShiptoCountryId(new Criteria(10));
        }
        return collCustomersRelatedByShiptoCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByShiptoCountryId(Criteria criteria) throws TorqueException
    {
              if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId() );
                        collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                            if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
                {
                    collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelect(criteria);
                }
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByShiptoCountryId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByShiptoCountryId(Connection con) throws TorqueException
    {
              if (collCustomersRelatedByShiptoCountryId == null)
        {
            collCustomersRelatedByShiptoCountryId = getCustomersRelatedByShiptoCountryId(new Criteria(10), con);
        }
        return collCustomersRelatedByShiptoCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByShiptoCountryId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                         criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                         collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                             if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
                 {
                     collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelect(criteria, con);
                 }
             }
         }
         lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

         return collCustomersRelatedByShiptoCountryId;
           }

                                                                                          
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinCustomerCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinIndustry(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinLeadSource(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinCountryRelatedByCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinRegionRelatedByRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinRegionRelatedByShiptoRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinRegionRelatedByShiptoRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinRegionRelatedByShiptoRegionId(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinEducationCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinHouseholdCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinAgeCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinRevenueCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getCustomersRelatedByShiptoCountryIdJoinEmployeeNoCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoCountryId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                              collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_COUNTRY_ID, getCountryId());
                                    if (!lastCustomersRelatedByShiptoCountryIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoCountryId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoCountryIdCriteria = criteria;

        return collCustomersRelatedByShiptoCountryId;
                }
                            


                                  
            
          /**
     * Collection to store aggregation of collContactsRelatedByCountryId
     */
    protected List collContactsRelatedByCountryId;

    /**
     * Temporary storage of collContactsRelatedByCountryId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initContactsRelatedByCountryId()
    {
        if (collContactsRelatedByCountryId == null)
        {
            collContactsRelatedByCountryId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Contact object to this object
     * through the Contact foreign key attribute
     *
     * @param l Contact
     * @throws TorqueException
     */
    public void addContactRelatedByCountryId(Contact l) throws TorqueException
    {
        getContactsRelatedByCountryId().add(l);
        l.setCountryRelatedByCountryId((Country) this);
    }

    /**
     * The criteria used to select the current contents of collContactsRelatedByCountryId
     */
    private Criteria lastContactsRelatedByCountryIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedByCountryId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getContactsRelatedByCountryId() throws TorqueException
    {
              if (collContactsRelatedByCountryId == null)
        {
            collContactsRelatedByCountryId = getContactsRelatedByCountryId(new Criteria(10));
        }
        return collContactsRelatedByCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getContactsRelatedByCountryId(Criteria criteria) throws TorqueException
    {
              if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId() );
                        collContactsRelatedByCountryId = ContactPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                            if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
                {
                    collContactsRelatedByCountryId = ContactPeer.doSelect(criteria);
                }
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedByCountryId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedByCountryId(Connection con) throws TorqueException
    {
              if (collContactsRelatedByCountryId == null)
        {
            collContactsRelatedByCountryId = getContactsRelatedByCountryId(new Criteria(10), con);
        }
        return collContactsRelatedByCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedByCountryId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                         criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                         collContactsRelatedByCountryId = ContactPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                             if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
                 {
                     collContactsRelatedByCountryId = ContactPeer.doSelect(criteria, con);
                 }
             }
         }
         lastContactsRelatedByCountryIdCriteria = criteria;

         return collContactsRelatedByCountryId;
           }

                                                                  
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinCustomer(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinSalutation(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinContactCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinRegionRelatedByRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinCountryRelatedBySecCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinCountryRelatedBySecCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinCountryRelatedBySecCountryId(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinRegionRelatedBySecRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinRegionRelatedBySecRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinRegionRelatedBySecRegionId(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedByCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedByCountryIdJoinJobPosition(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedByCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                              collContactsRelatedByCountryId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedByCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedByCountryId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        lastContactsRelatedByCountryIdCriteria = criteria;

        return collContactsRelatedByCountryId;
                }
                            


                                  
            
          /**
     * Collection to store aggregation of collContactsRelatedBySecCountryId
     */
    protected List collContactsRelatedBySecCountryId;

    /**
     * Temporary storage of collContactsRelatedBySecCountryId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initContactsRelatedBySecCountryId()
    {
        if (collContactsRelatedBySecCountryId == null)
        {
            collContactsRelatedBySecCountryId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Contact object to this object
     * through the Contact foreign key attribute
     *
     * @param l Contact
     * @throws TorqueException
     */
    public void addContactRelatedBySecCountryId(Contact l) throws TorqueException
    {
        getContactsRelatedBySecCountryId().add(l);
        l.setCountryRelatedBySecCountryId((Country) this);
    }

    /**
     * The criteria used to select the current contents of collContactsRelatedBySecCountryId
     */
    private Criteria lastContactsRelatedBySecCountryIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedBySecCountryId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getContactsRelatedBySecCountryId() throws TorqueException
    {
              if (collContactsRelatedBySecCountryId == null)
        {
            collContactsRelatedBySecCountryId = getContactsRelatedBySecCountryId(new Criteria(10));
        }
        return collContactsRelatedBySecCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getContactsRelatedBySecCountryId(Criteria criteria) throws TorqueException
    {
              if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId() );
                        collContactsRelatedBySecCountryId = ContactPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                            if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
                {
                    collContactsRelatedBySecCountryId = ContactPeer.doSelect(criteria);
                }
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedBySecCountryId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedBySecCountryId(Connection con) throws TorqueException
    {
              if (collContactsRelatedBySecCountryId == null)
        {
            collContactsRelatedBySecCountryId = getContactsRelatedBySecCountryId(new Criteria(10), con);
        }
        return collContactsRelatedBySecCountryId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedBySecCountryId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                         criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                         collContactsRelatedBySecCountryId = ContactPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                             if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
                 {
                     collContactsRelatedBySecCountryId = ContactPeer.doSelect(criteria, con);
                 }
             }
         }
         lastContactsRelatedBySecCountryIdCriteria = criteria;

         return collContactsRelatedBySecCountryId;
           }

                                                                  
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinCustomer(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinSalutation(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinContactCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinCountryRelatedByCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinRegionRelatedByRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinRegionRelatedBySecRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinRegionRelatedBySecRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinRegionRelatedBySecRegionId(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related ContactsRelatedBySecCountryId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getContactsRelatedBySecCountryIdJoinJobPosition(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecCountryId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecCountryId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                              collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_COUNTRY_ID, getCountryId());
                                    if (!lastContactsRelatedBySecCountryIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecCountryId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        lastContactsRelatedBySecCountryIdCriteria = criteria;

        return collContactsRelatedBySecCountryId;
                }
                            


                          
            
          /**
     * Collection to store aggregation of collNewsletters
     */
    protected List collNewsletters;

    /**
     * Temporary storage of collNewsletters to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initNewsletters()
    {
        if (collNewsletters == null)
        {
            collNewsletters = new ArrayList();
        }
    }

    /**
     * Method called to associate a Newsletter object to this object
     * through the Newsletter foreign key attribute
     *
     * @param l Newsletter
     * @throws TorqueException
     */
    public void addNewsletter(Newsletter l) throws TorqueException
    {
        getNewsletters().add(l);
        l.setCountry((Country) this);
    }

    /**
     * The criteria used to select the current contents of collNewsletters
     */
    private Criteria lastNewslettersCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getNewsletters(new Criteria())
     *
     * @throws TorqueException
     */
    public List getNewsletters() throws TorqueException
    {
              if (collNewsletters == null)
        {
            collNewsletters = getNewsletters(new Criteria(10));
        }
        return collNewsletters;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getNewsletters(Criteria criteria) throws TorqueException
    {
              if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId() );
                        collNewsletters = NewsletterPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                            if (!lastNewslettersCriteria.equals(criteria))
                {
                    collNewsletters = NewsletterPeer.doSelect(criteria);
                }
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getNewsletters(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getNewsletters(Connection con) throws TorqueException
    {
              if (collNewsletters == null)
        {
            collNewsletters = getNewsletters(new Criteria(10), con);
        }
        return collNewsletters;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     * If this Country is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getNewsletters(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                         criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                         collNewsletters = NewsletterPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                             if (!lastNewslettersCriteria.equals(criteria))
                 {
                     collNewsletters = NewsletterPeer.doSelect(criteria, con);
                 }
             }
         }
         lastNewslettersCriteria = criteria;

         return collNewsletters;
           }

                                                            
              
                    
                    
                                
                                                              
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinCustomerCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinProjectRelatedByProjectId(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinProjectRelatedByProjectId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinProjectRelatedByProjectId(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinProductRelatedByProductId(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinProductRelatedByProductId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinProductRelatedByProductId(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinProjectRelatedByRelProjectId(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinProjectRelatedByRelProjectId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinProjectRelatedByRelProjectId(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinProductRelatedByRelProductId(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinProductRelatedByRelProductId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinProductRelatedByRelProductId(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                  
                    
                              
                                
                                                              
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinCountry(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinCountry(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinCountry(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinLanguageRelatedByCustLanguageId(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinLanguageRelatedByCustLanguageId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinLanguageRelatedByCustLanguageId(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                    
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Country is new, it will return
     * an empty collection; or if this Country has previously
     * been saved, it will retrieve related Newsletters from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Country.
     */
    protected List getNewslettersJoinLanguageRelatedByLanguageId(Criteria criteria)
        throws TorqueException
    {
                    if (collNewsletters == null)
        {
            if (isNew())
            {
               collNewsletters = new ArrayList();
            }
            else
            {
                              criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                              collNewsletters = NewsletterPeer.doSelectJoinLanguageRelatedByLanguageId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(NewsletterPeer.CUST_COUNTRY_ID, getCountryId());
                                    if (!lastNewslettersCriteria.equals(criteria))
            {
                collNewsletters = NewsletterPeer.doSelectJoinLanguageRelatedByLanguageId(criteria);
            }
        }
        lastNewslettersCriteria = criteria;

        return collNewsletters;
                }
                            


          
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
              fieldNames.add("CountryId");
              fieldNames.add("CountryName");
              fieldNames.add("CountryCode");
              fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
          if (name.equals("CountryId"))
        {
                return new Integer(getCountryId());
            }
          if (name.equals("CountryName"))
        {
                return getCountryName();
            }
          if (name.equals("CountryCode"))
        {
                return getCountryCode();
            }
          return null;
    }
    
    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
          if (name.equals(CountryPeer.COUNTRY_ID))
        {
                return new Integer(getCountryId());
            }
          if (name.equals(CountryPeer.COUNTRY_NAME))
        {
                return getCountryName();
            }
          if (name.equals(CountryPeer.COUNTRY_CODE))
        {
                return getCountryCode();
            }
          return null;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
            if (pos == 0)
        {
                return new Integer(getCountryId());
            }
              if (pos == 1)
        {
                return getCountryName();
            }
              if (pos == 2)
        {
                return getCountryCode();
            }
              return null;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
          save(CountryPeer.getMapBuilder()
                .getDatabaseMap().getName());
      }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
       * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
       *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
          try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
      }

      /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
          if (!alreadyInSave)
        {
            alreadyInSave = true;


  
            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    CountryPeer.doInsert((Country) this, con);
                    setNew(false);
                }
                else
                {
                    CountryPeer.doUpdate((Country) this, con);
                }
            }

                                                
                
                    if (collCustomersRelatedByCountryId != null)
            {
                for (int i = 0; i < collCustomersRelatedByCountryId.size(); i++)
                {
                    ((Customer) collCustomersRelatedByCountryId.get(i)).save(con);
                }
            }
                                                            
                
                    if (collCustomersRelatedByShiptoCountryId != null)
            {
                for (int i = 0; i < collCustomersRelatedByShiptoCountryId.size(); i++)
                {
                    ((Customer) collCustomersRelatedByShiptoCountryId.get(i)).save(con);
                }
            }
                                                            
                
                    if (collContactsRelatedByCountryId != null)
            {
                for (int i = 0; i < collContactsRelatedByCountryId.size(); i++)
                {
                    ((Contact) collContactsRelatedByCountryId.get(i)).save(con);
                }
            }
                                                            
                
                    if (collContactsRelatedBySecCountryId != null)
            {
                for (int i = 0; i < collContactsRelatedBySecCountryId.size(); i++)
                {
                    ((Contact) collContactsRelatedBySecCountryId.get(i)).save(con);
                }
            }
                                                  
                
                    if (collNewsletters != null)
            {
                for (int i = 0; i < collNewsletters.size(); i++)
                {
                    ((Newsletter) collNewsletters.get(i)).save(con);
                }
            }
                                  alreadyInSave = false;
        }
      }

                        
      /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param key countryId ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        throws TorqueException
    {
            setCountryId(((NumberKey) key).intValue());
        }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
            setCountryId(Integer.parseInt(key));
        }

  
    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
          return SimpleKey.keyFor(getCountryId());
      }
 
    /**
     * get an id that differentiates this object from others
     * of its class.
     */
    public String getQueryKey()
    {
        if (getPrimaryKey() == null)
        {
            return "";
        }
        else
        {
            return getPrimaryKey().toString();
        }
    }

    /**
     * set an id that differentiates this object from others
     * of its class.
     */
    public void setQueryKey(String key)
        throws TorqueException
    {
        setPrimaryKey(key);
    }

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
       * It then fills all the association collections and sets the
     * related objects to isNew=true.
       */
      public Country copy() throws TorqueException
    {
        return copyInto(new Country());
    }
  
    protected Country copyInto(Country copyObj) throws TorqueException
    {
          copyObj.setCountryId(countryId);
          copyObj.setCountryName(countryName);
          copyObj.setCountryCode(countryCode);
  
                            copyObj.setCountryId( 0);
                        
                                                
                            
        List v = getCustomersRelatedByCountryId();
        for (int i = 0; i < v.size(); i++)
        {
            Customer obj = (Customer) v.get(i);
            copyObj.addCustomerRelatedByCountryId(obj.copy());
        }
                                                            
                            
        v = getCustomersRelatedByShiptoCountryId();
        for (int i = 0; i < v.size(); i++)
        {
            Customer obj = (Customer) v.get(i);
            copyObj.addCustomerRelatedByShiptoCountryId(obj.copy());
        }
                                                            
                            
        v = getContactsRelatedByCountryId();
        for (int i = 0; i < v.size(); i++)
        {
            Contact obj = (Contact) v.get(i);
            copyObj.addContactRelatedByCountryId(obj.copy());
        }
                                                            
                            
        v = getContactsRelatedBySecCountryId();
        for (int i = 0; i < v.size(); i++)
        {
            Contact obj = (Contact) v.get(i);
            copyObj.addContactRelatedBySecCountryId(obj.copy());
        }
                                                  
                            
        v = getNewsletters();
        for (int i = 0; i < v.size(); i++)
        {
            Newsletter obj = (Newsletter) v.get(i);
            copyObj.addNewsletter(obj.copy());
        }
                            return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public CountryPeer getPeer()
    {
        return peer;
    }

    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("Country:\n");
        str.append("CountryId = ")
               .append(getCountryId())
             .append("\n");
        str.append("CountryName = ")
               .append(getCountryName())
             .append("\n");
        str.append("CountryCode = ")
               .append(getCountryCode())
             .append("\n");
        return(str.toString());
    }
}
