package org.campware.cream.om;


import java.math.BigDecimal;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Date;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.turbine.om.Retrievable;
import org.apache.torque.TorqueException;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;


/**
 * This class was autogenerated by Torque on:
 *
 * [Fri Jan 26 03:28:50 CET 2007]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to Region
 */
public abstract class BaseRegion extends BaseObject
    implements org.apache.turbine.om.Retrievable
{
    /** The Peer class */
    private static final RegionPeer peer =
        new RegionPeer();

        
    /** The value for the regionId field */
    private int regionId;
      
    /** The value for the regionName field */
    private String regionName;
  
    
    /**
     * Get the RegionId
     *
     * @return int
     */
    public int getRegionId()
    {
        return regionId;
    }

                                              
    /**
     * Set the value of RegionId
     *
     * @param v new value
     */
    public void setRegionId(int v) throws TorqueException
    {
    
                  if (this.regionId != v)
              {
            this.regionId = v;
            setModified(true);
        }
    
          
                                  
                  // update associated Customer
        if (collCustomersRelatedByRegionId != null)
        {
            for (int i = 0; i < collCustomersRelatedByRegionId.size(); i++)
            {
                ((Customer) collCustomersRelatedByRegionId.get(i))
                    .setRegionId(v);
            }
        }
                                                    
                  // update associated Customer
        if (collCustomersRelatedByShiptoRegionId != null)
        {
            for (int i = 0; i < collCustomersRelatedByShiptoRegionId.size(); i++)
            {
                ((Customer) collCustomersRelatedByShiptoRegionId.get(i))
                    .setShiptoRegionId(v);
            }
        }
                                                    
                  // update associated Contact
        if (collContactsRelatedByRegionId != null)
        {
            for (int i = 0; i < collContactsRelatedByRegionId.size(); i++)
            {
                ((Contact) collContactsRelatedByRegionId.get(i))
                    .setRegionId(v);
            }
        }
                                                    
                  // update associated Contact
        if (collContactsRelatedBySecRegionId != null)
        {
            for (int i = 0; i < collContactsRelatedBySecRegionId.size(); i++)
            {
                ((Contact) collContactsRelatedBySecRegionId.get(i))
                    .setSecRegionId(v);
            }
        }
                                }
  
    /**
     * Get the RegionName
     *
     * @return String
     */
    public String getRegionName()
    {
        return regionName;
    }

                        
    /**
     * Set the value of RegionName
     *
     * @param v new value
     */
    public void setRegionName(String v) 
    {
    
                  if (!ObjectUtils.equals(this.regionName, v))
              {
            this.regionName = v;
            setModified(true);
        }
    
          
              }
  
         
                                        
            
          /**
     * Collection to store aggregation of collCustomersRelatedByRegionId
     */
    protected List collCustomersRelatedByRegionId;

    /**
     * Temporary storage of collCustomersRelatedByRegionId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initCustomersRelatedByRegionId()
    {
        if (collCustomersRelatedByRegionId == null)
        {
            collCustomersRelatedByRegionId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Customer object to this object
     * through the Customer foreign key attribute
     *
     * @param l Customer
     * @throws TorqueException
     */
    public void addCustomerRelatedByRegionId(Customer l) throws TorqueException
    {
        getCustomersRelatedByRegionId().add(l);
        l.setRegionRelatedByRegionId((Region) this);
    }

    /**
     * The criteria used to select the current contents of collCustomersRelatedByRegionId
     */
    private Criteria lastCustomersRelatedByRegionIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByRegionId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByRegionId() throws TorqueException
    {
              if (collCustomersRelatedByRegionId == null)
        {
            collCustomersRelatedByRegionId = getCustomersRelatedByRegionId(new Criteria(10));
        }
        return collCustomersRelatedByRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByRegionId(Criteria criteria) throws TorqueException
    {
              if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                        criteria.add(CustomerPeer.REGION_ID, getRegionId() );
                        collCustomersRelatedByRegionId = CustomerPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(CustomerPeer.REGION_ID, getRegionId());
                            if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
                {
                    collCustomersRelatedByRegionId = CustomerPeer.doSelect(criteria);
                }
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByRegionId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByRegionId(Connection con) throws TorqueException
    {
              if (collCustomersRelatedByRegionId == null)
        {
            collCustomersRelatedByRegionId = getCustomersRelatedByRegionId(new Criteria(10), con);
        }
        return collCustomersRelatedByRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByRegionId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                         criteria.add(CustomerPeer.REGION_ID, getRegionId());
                         collCustomersRelatedByRegionId = CustomerPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                             if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
                 {
                     collCustomersRelatedByRegionId = CustomerPeer.doSelect(criteria, con);
                 }
             }
         }
         lastCustomersRelatedByRegionIdCriteria = criteria;

         return collCustomersRelatedByRegionId;
           }

                                                                                          
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinCustomerCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinIndustry(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinLeadSource(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinCountryRelatedByCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinCountryRelatedByShiptoCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinCountryRelatedByShiptoCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinCountryRelatedByShiptoCountryId(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinRegionRelatedByShiptoRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinRegionRelatedByShiptoRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinRegionRelatedByShiptoRegionId(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinEducationCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinHouseholdCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinAgeCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinRevenueCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByRegionIdJoinEmployeeNoCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.REGION_ID, getRegionId());
                              collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByRegionId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        lastCustomersRelatedByRegionIdCriteria = criteria;

        return collCustomersRelatedByRegionId;
                }
                            


                                  
            
          /**
     * Collection to store aggregation of collCustomersRelatedByShiptoRegionId
     */
    protected List collCustomersRelatedByShiptoRegionId;

    /**
     * Temporary storage of collCustomersRelatedByShiptoRegionId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initCustomersRelatedByShiptoRegionId()
    {
        if (collCustomersRelatedByShiptoRegionId == null)
        {
            collCustomersRelatedByShiptoRegionId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Customer object to this object
     * through the Customer foreign key attribute
     *
     * @param l Customer
     * @throws TorqueException
     */
    public void addCustomerRelatedByShiptoRegionId(Customer l) throws TorqueException
    {
        getCustomersRelatedByShiptoRegionId().add(l);
        l.setRegionRelatedByShiptoRegionId((Region) this);
    }

    /**
     * The criteria used to select the current contents of collCustomersRelatedByShiptoRegionId
     */
    private Criteria lastCustomersRelatedByShiptoRegionIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByShiptoRegionId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByShiptoRegionId() throws TorqueException
    {
              if (collCustomersRelatedByShiptoRegionId == null)
        {
            collCustomersRelatedByShiptoRegionId = getCustomersRelatedByShiptoRegionId(new Criteria(10));
        }
        return collCustomersRelatedByShiptoRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getCustomersRelatedByShiptoRegionId(Criteria criteria) throws TorqueException
    {
              if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId() );
                        collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                            if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
                {
                    collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelect(criteria);
                }
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getCustomersRelatedByShiptoRegionId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByShiptoRegionId(Connection con) throws TorqueException
    {
              if (collCustomersRelatedByShiptoRegionId == null)
        {
            collCustomersRelatedByShiptoRegionId = getCustomersRelatedByShiptoRegionId(new Criteria(10), con);
        }
        return collCustomersRelatedByShiptoRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getCustomersRelatedByShiptoRegionId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                         criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                         collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                             if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
                 {
                     collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelect(criteria, con);
                 }
             }
         }
         lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

         return collCustomersRelatedByShiptoRegionId;
           }

                                                                                          
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinCustomerCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinCustomerCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinIndustry(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinIndustry(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinLeadSource(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinLeadSource(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinCountryRelatedByCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinRegionRelatedByRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinCountryRelatedByShiptoCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinCountryRelatedByShiptoCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinCountryRelatedByShiptoCountryId(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinEducationCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinEducationCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinHouseholdCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinHouseholdCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinAgeCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinAgeCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinRevenueCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinRevenueCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related CustomersRelatedByShiptoRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getCustomersRelatedByShiptoRegionIdJoinEmployeeNoCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collCustomersRelatedByShiptoRegionId == null)
        {
            if (isNew())
            {
               collCustomersRelatedByShiptoRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                              collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(CustomerPeer.SHIPTO_REGION_ID, getRegionId());
                                    if (!lastCustomersRelatedByShiptoRegionIdCriteria.equals(criteria))
            {
                collCustomersRelatedByShiptoRegionId = CustomerPeer.doSelectJoinEmployeeNoCategory(criteria);
            }
        }
        lastCustomersRelatedByShiptoRegionIdCriteria = criteria;

        return collCustomersRelatedByShiptoRegionId;
                }
                            


                                  
            
          /**
     * Collection to store aggregation of collContactsRelatedByRegionId
     */
    protected List collContactsRelatedByRegionId;

    /**
     * Temporary storage of collContactsRelatedByRegionId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initContactsRelatedByRegionId()
    {
        if (collContactsRelatedByRegionId == null)
        {
            collContactsRelatedByRegionId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Contact object to this object
     * through the Contact foreign key attribute
     *
     * @param l Contact
     * @throws TorqueException
     */
    public void addContactRelatedByRegionId(Contact l) throws TorqueException
    {
        getContactsRelatedByRegionId().add(l);
        l.setRegionRelatedByRegionId((Region) this);
    }

    /**
     * The criteria used to select the current contents of collContactsRelatedByRegionId
     */
    private Criteria lastContactsRelatedByRegionIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedByRegionId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getContactsRelatedByRegionId() throws TorqueException
    {
              if (collContactsRelatedByRegionId == null)
        {
            collContactsRelatedByRegionId = getContactsRelatedByRegionId(new Criteria(10));
        }
        return collContactsRelatedByRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getContactsRelatedByRegionId(Criteria criteria) throws TorqueException
    {
              if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                        criteria.add(ContactPeer.REGION_ID, getRegionId() );
                        collContactsRelatedByRegionId = ContactPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(ContactPeer.REGION_ID, getRegionId());
                            if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
                {
                    collContactsRelatedByRegionId = ContactPeer.doSelect(criteria);
                }
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedByRegionId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedByRegionId(Connection con) throws TorqueException
    {
              if (collContactsRelatedByRegionId == null)
        {
            collContactsRelatedByRegionId = getContactsRelatedByRegionId(new Criteria(10), con);
        }
        return collContactsRelatedByRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedByRegionId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                         criteria.add(ContactPeer.REGION_ID, getRegionId());
                         collContactsRelatedByRegionId = ContactPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                             if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
                 {
                     collContactsRelatedByRegionId = ContactPeer.doSelect(criteria, con);
                 }
             }
         }
         lastContactsRelatedByRegionIdCriteria = criteria;

         return collContactsRelatedByRegionId;
           }

                                                                  
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinCustomer(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinSalutation(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinContactCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinCountryRelatedByCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinCountryRelatedBySecCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinCountryRelatedBySecCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinCountryRelatedBySecCountryId(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinRegionRelatedBySecRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinRegionRelatedBySecRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinRegionRelatedBySecRegionId(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedByRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedByRegionIdJoinJobPosition(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedByRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedByRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.REGION_ID, getRegionId());
                              collContactsRelatedByRegionId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.REGION_ID, getRegionId());
                                    if (!lastContactsRelatedByRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedByRegionId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        lastContactsRelatedByRegionIdCriteria = criteria;

        return collContactsRelatedByRegionId;
                }
                            


                                  
            
          /**
     * Collection to store aggregation of collContactsRelatedBySecRegionId
     */
    protected List collContactsRelatedBySecRegionId;

    /**
     * Temporary storage of collContactsRelatedBySecRegionId to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initContactsRelatedBySecRegionId()
    {
        if (collContactsRelatedBySecRegionId == null)
        {
            collContactsRelatedBySecRegionId = new ArrayList();
        }
    }

    /**
     * Method called to associate a Contact object to this object
     * through the Contact foreign key attribute
     *
     * @param l Contact
     * @throws TorqueException
     */
    public void addContactRelatedBySecRegionId(Contact l) throws TorqueException
    {
        getContactsRelatedBySecRegionId().add(l);
        l.setRegionRelatedBySecRegionId((Region) this);
    }

    /**
     * The criteria used to select the current contents of collContactsRelatedBySecRegionId
     */
    private Criteria lastContactsRelatedBySecRegionIdCriteria = null;
      
    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedBySecRegionId(new Criteria())
     *
     * @throws TorqueException
     */
    public List getContactsRelatedBySecRegionId() throws TorqueException
    {
              if (collContactsRelatedBySecRegionId == null)
        {
            collContactsRelatedBySecRegionId = getContactsRelatedBySecRegionId(new Criteria(10));
        }
        return collContactsRelatedBySecRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getContactsRelatedBySecRegionId(Criteria criteria) throws TorqueException
    {
              if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId() );
                        collContactsRelatedBySecRegionId = ContactPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                            criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                            if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
                {
                    collContactsRelatedBySecRegionId = ContactPeer.doSelect(criteria);
                }
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
          }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getContactsRelatedBySecRegionId(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedBySecRegionId(Connection con) throws TorqueException
    {
              if (collContactsRelatedBySecRegionId == null)
        {
            collContactsRelatedBySecRegionId = getContactsRelatedBySecRegionId(new Criteria(10), con);
        }
        return collContactsRelatedBySecRegionId;
          }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     * If this Region is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getContactsRelatedBySecRegionId(Criteria criteria, Connection con)
            throws TorqueException
    {
              if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                         criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                         collContactsRelatedBySecRegionId = ContactPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                             if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
                 {
                     collContactsRelatedBySecRegionId = ContactPeer.doSelect(criteria, con);
                 }
             }
         }
         lastContactsRelatedBySecRegionIdCriteria = criteria;

         return collContactsRelatedBySecRegionId;
           }

                                                                  
              
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinCustomer(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinCustomer(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinSalutation(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinSalutation(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinContactCategory(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinContactCategory(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinCountryRelatedByCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinCountryRelatedByCountryId(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinRegionRelatedByRegionId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinRegionRelatedByRegionId(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                  
                    
                    
                                            
                                                                          
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinCountryRelatedBySecCountryId(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinCountryRelatedBySecCountryId(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinCountryRelatedBySecCountryId(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                  
                    
                              
                                            
                                                                          
                                        
                    
                                            
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinLanguage(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinLanguage(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                  
                    
                    
                                
                                                              
                                        
                    
                                
          
    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Region is new, it will return
     * an empty collection; or if this Region has previously
     * been saved, it will retrieve related ContactsRelatedBySecRegionId from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Region.
     */
    protected List getContactsRelatedBySecRegionIdJoinJobPosition(Criteria criteria)
        throws TorqueException
    {
                    if (collContactsRelatedBySecRegionId == null)
        {
            if (isNew())
            {
               collContactsRelatedBySecRegionId = new ArrayList();
            }
            else
            {
                              criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                              collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        else
        {
            // the following code is to determine if a new query is
            // called for.  If the criteria is the same as the last
            // one, just return the collection.
            
                        criteria.add(ContactPeer.SEC_REGION_ID, getRegionId());
                                    if (!lastContactsRelatedBySecRegionIdCriteria.equals(criteria))
            {
                collContactsRelatedBySecRegionId = ContactPeer.doSelectJoinJobPosition(criteria);
            }
        }
        lastContactsRelatedBySecRegionIdCriteria = criteria;

        return collContactsRelatedBySecRegionId;
                }
                            


          
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
              fieldNames.add("RegionId");
              fieldNames.add("RegionName");
              fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
          if (name.equals("RegionId"))
        {
                return new Integer(getRegionId());
            }
          if (name.equals("RegionName"))
        {
                return getRegionName();
            }
          return null;
    }
    
    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
          if (name.equals(RegionPeer.REGION_ID))
        {
                return new Integer(getRegionId());
            }
          if (name.equals(RegionPeer.REGION_NAME))
        {
                return getRegionName();
            }
          return null;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
            if (pos == 0)
        {
                return new Integer(getRegionId());
            }
              if (pos == 1)
        {
                return getRegionName();
            }
              return null;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
          save(RegionPeer.getMapBuilder()
                .getDatabaseMap().getName());
      }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
       * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
       *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
          try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
      }

      /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
          if (!alreadyInSave)
        {
            alreadyInSave = true;


  
            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    RegionPeer.doInsert((Region) this, con);
                    setNew(false);
                }
                else
                {
                    RegionPeer.doUpdate((Region) this, con);
                }
            }

                                                
                
                    if (collCustomersRelatedByRegionId != null)
            {
                for (int i = 0; i < collCustomersRelatedByRegionId.size(); i++)
                {
                    ((Customer) collCustomersRelatedByRegionId.get(i)).save(con);
                }
            }
                                                            
                
                    if (collCustomersRelatedByShiptoRegionId != null)
            {
                for (int i = 0; i < collCustomersRelatedByShiptoRegionId.size(); i++)
                {
                    ((Customer) collCustomersRelatedByShiptoRegionId.get(i)).save(con);
                }
            }
                                                            
                
                    if (collContactsRelatedByRegionId != null)
            {
                for (int i = 0; i < collContactsRelatedByRegionId.size(); i++)
                {
                    ((Contact) collContactsRelatedByRegionId.get(i)).save(con);
                }
            }
                                                            
                
                    if (collContactsRelatedBySecRegionId != null)
            {
                for (int i = 0; i < collContactsRelatedBySecRegionId.size(); i++)
                {
                    ((Contact) collContactsRelatedBySecRegionId.get(i)).save(con);
                }
            }
                                  alreadyInSave = false;
        }
      }

                        
      /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param key regionId ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        throws TorqueException
    {
            setRegionId(((NumberKey) key).intValue());
        }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
            setRegionId(Integer.parseInt(key));
        }

  
    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
          return SimpleKey.keyFor(getRegionId());
      }
 
    /**
     * get an id that differentiates this object from others
     * of its class.
     */
    public String getQueryKey()
    {
        if (getPrimaryKey() == null)
        {
            return "";
        }
        else
        {
            return getPrimaryKey().toString();
        }
    }

    /**
     * set an id that differentiates this object from others
     * of its class.
     */
    public void setQueryKey(String key)
        throws TorqueException
    {
        setPrimaryKey(key);
    }

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
       * It then fills all the association collections and sets the
     * related objects to isNew=true.
       */
      public Region copy() throws TorqueException
    {
        return copyInto(new Region());
    }
  
    protected Region copyInto(Region copyObj) throws TorqueException
    {
          copyObj.setRegionId(regionId);
          copyObj.setRegionName(regionName);
  
                            copyObj.setRegionId( 0);
                  
                                                
                            
        List v = getCustomersRelatedByRegionId();
        for (int i = 0; i < v.size(); i++)
        {
            Customer obj = (Customer) v.get(i);
            copyObj.addCustomerRelatedByRegionId(obj.copy());
        }
                                                            
                            
        v = getCustomersRelatedByShiptoRegionId();
        for (int i = 0; i < v.size(); i++)
        {
            Customer obj = (Customer) v.get(i);
            copyObj.addCustomerRelatedByShiptoRegionId(obj.copy());
        }
                                                            
                            
        v = getContactsRelatedByRegionId();
        for (int i = 0; i < v.size(); i++)
        {
            Contact obj = (Contact) v.get(i);
            copyObj.addContactRelatedByRegionId(obj.copy());
        }
                                                            
                            
        v = getContactsRelatedBySecRegionId();
        for (int i = 0; i < v.size(); i++)
        {
            Contact obj = (Contact) v.get(i);
            copyObj.addContactRelatedBySecRegionId(obj.copy());
        }
                            return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public RegionPeer getPeer()
    {
        return peer;
    }

    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("Region:\n");
        str.append("RegionId = ")
               .append(getRegionId())
             .append("\n");
        str.append("RegionName = ")
               .append(getRegionName())
             .append("\n");
        return(str.toString());
    }
}
